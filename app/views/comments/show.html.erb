<ol>
    <li>
    <div id="cheader">
        <span id="cauthor">
            <%=h @comment.author %>
        </span>
        <% if session[:logged_in] %>
            <%= link_to 'Edit', :controller => 'comments', :action => 'edit', :id => @comment.id %>
            <%= link_to 'Delete', @comment, :confirm => 'Are you sure?', :method => 'delete' %>
        <% end %>
    </div>
    <%= @comment.content -%>
    </li>
</ol>
<%= link_to 'Back', :controller => 'posts', :action => 'show', :id => @comment.post_id %>
